#include "subarctic/pinctrl.h"
#include "bone/pins.h"

&dcan0 {
	pinctrl-names = "default";
	pinctrl-0 = <&can0_pins>;

	status = "okay";
};

&pinctrl {
	can0_pins: can0 {
		pinctrl-single,pins = <
			PIN_IN_PULLUP(  P9_19, 2 )  // rx
			PIN_OUT_PULLUP( P9_20, 2 )  // tx
		>;
	};
};
